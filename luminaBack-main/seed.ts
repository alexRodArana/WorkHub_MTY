/**
 * Official production seed entrypoint for WorkHub MTY.
 *
 * This delegates to the deterministic production demo seed, which prepares the
 * data used by the deployed product demo: users, profiles, vehicles,
 * reservations, badges and operational blocks.
 */
import "./seed_production_demo_data"
